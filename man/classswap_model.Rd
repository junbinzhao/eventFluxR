% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classswap_model.R
\name{classswap_model}
\alias{classswap_model}
\title{Fit a class-swap model (hot-moment classifier + separate regressors)}
\usage{
classswap_model(
  data,
  response_col,
  feature_cols,
  group_vars,
  label_col = NULL,
  mad_k = 3,
  threshold = 0.65,
  weight_exponent = 0,
  weight_q = 0.95,
  n_trials = 100,
  nrounds = 3000,
  early_stopping_rounds = 100,
  seed = 23,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame containing the response, predictors and
grouping columns.}

\item{response_col}{Name of the numeric flux response column.}

\item{feature_cols}{Character vector of predictor column names (used by
both the classifier and the two regressors).}

\item{group_vars}{Character vector of grouping columns (e.g.
`c("Plot", "Fertiliz")`) defining blocked cross-validation folds.}

\item{label_col}{Optional name of an existing binary hot-moment label
used to train the classifier; if `NULL`, a MAD flag (`mad_k`) on
`response_col` is used instead.}

\item{mad_k}{MAD threshold multiplier used to derive the label when
`label_col` is `NULL` (default 3).}

\item{threshold}{Classifier probability threshold for calling a hot
moment (default 0.65).}

\item{weight_exponent, weight_q}{Control up-weighting of large flux values
when training the hot-moment regressor:
`weight = max(1, (max(y, 0) / q)^exponent)`, where `q` is the
`weight_q` quantile of `response_col` (default exponent 0 = uniform
weights).}

\item{n_trials, nrounds, early_stopping_rounds, seed, verbose}{Passed to the
XGBoost random-search tuner used for the classifier and both
regressors.}
}
\value{
An object of class `"classswap_model"` with elements
  `classifier`, `model_bg`, `model_hot`, `feature_cols`, `threshold`, and
  `cv_scores` (best cross-validation score for each of the three fitted
  models). Use `predict()` (see [predict.classswap_model()]) to generate
  recombined predictions on new data.
}
\description{
Implements the "class-swap" workflow used for N2O flux modeling: an
XGBoost classifier first labels each observation as background or hot
moment, then independent XGBoost regressors are fit within each class
(optionally up-weighting large flux values), so predictions can be
recombined by routing each observation through its classified branch
(see [predict.classswap_model()]).
}
\examples{
\donttest{
data(n2o_sample)
feats <- c("CO2_flux", "Soil_Moist", "Soil_T_2", "VPD", "fert_ef")

fit <- classswap_model(
  n2o_sample, response_col = "N2O_flux", feature_cols = feats,
  group_vars = "Chamber", label_col = "y_bin",
  n_trials = 3, nrounds = 50, early_stopping_rounds = 10, verbose = FALSE
)
pred <- predict(fit, n2o_sample)
head(pred)
}
}
