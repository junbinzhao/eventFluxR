% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fertilization_index.R
\name{fertilization_index}
\alias{fertilization_index}
\title{Compute a decay-weighted fertilization index}
\usage{
fertilization_index(
  timestamps,
  event_times,
  event_doses,
  k = 0.05,
  half_life = NULL,
  units = "days"
)
}
\arguments{
\item{timestamps}{Vector of times (`Date`/`POSIXct`/numeric) at which to
evaluate the index.}

\item{event_times}{Vector of fertilization event times (same class
family as `timestamps`).}

\item{event_doses}{Numeric vector of doses applied at `event_times` (e.g.
kg N ha-1); recycled if length 1.}

\item{k}{Decay rate constant per unit of `units` (default 0.05/day;
half-life = `log(2)/k`). Ignored if `half_life` is supplied.}

\item{half_life}{Optional half-life (same unit as `units`); if supplied,
overrides `k` via `k = log(2) / half_life`.}

\item{units}{Units passed to `difftime()` (default `"days"`).}
}
\value{
A data frame with one row per `timestamps` value, containing
  `fert_index` (the decayed, summed dose) and `days_since_fertiliz`
  (days since the closest prior event, `NA` if none had occurred yet).
}
\description{
For each timestamp, sums the exponentially decayed contribution of every
past fertilization event (`dose * exp(-k * days_since)`), and reports the
days elapsed since the most recent event. Generalizes the "fert_ef" /
"D_fertiliz" variables used in the Svanhovd N2O flux models.
}
\examples{
data(n2o_sample)

# two fertilization events during the 2022 season at Svanhovd (High plots)
events <- as.POSIXct(c("2022-05-02", "2022-08-09"), tz = "UTC")
idx <- fertilization_index(
  n2o_sample$Timestamp, events,
  event_doses = c(90, 36), half_life = 14
)
head(idx)
}
